# render.yaml  (Blueprint)
services:
  - type: web
    name: socialdemo-backend
    env: docker
    plan: free
    region: singapore   # 可改 closest 區域
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    healthCheckPath: /healthz
    disks:
      - name: data
        mountPath: /data
        sizeGB: 1
